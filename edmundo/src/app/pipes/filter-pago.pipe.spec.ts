import { FilterPagoPipe } from './filter-pago.pipe';

describe('FilterPagoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPagoPipe();
    expect(pipe).toBeTruthy();
  });
});
